# 학습 로그

이 파일은 개발 세션이 끝날 때마다 `tools/record-learning.ps1`로 누적 기록한다.

## 2026-04-26 초기 학습 기록

- 현재 안정 기준은 `v4.8.30 Phone Zone Label Fit`.
- GitHub Pages와 로컬 실행 모두 `v4.8.30`, `Phone Zone Label Fit`, `latest.generated.patch.js`가 확인됨.
- 새 챗버전 자료 `v4.8.31~v4.8.35`는 문서/후보 패치/데이터 초안으로 통합됨.
- 버들치 카드/팝업 PNG는 아직 없으므로 SVG fallback과 기존 4방향 PNG를 유지해야 함.
- 다음 핵심 목표는 모바일 메뉴 중복 CSS 제거, 물고기 데이터 정규화, 버들치 에셋 완성이다.

## 2026-04-26 10:56:45

- Summary: 프로젝트 전용 학습 시스템을 추가해 ChatGPT/Codex가 세션 시작 시 같은 기억, 규칙, 결정 로그를 읽고 이어받게 했다.
- Next: 다음 개발부터 record-learning으로 세션 결과를 누적하고, 모바일 메뉴 최적화 후보를 중복 없이 runtime patch로 통합한다.
